import React from "react";
import renderer from "react-test-renderer";

var filledSquares;
var filledSquaresAfterReversal;

describe('Reverse Time', () => {
    it('Test click event', () => {
      // when someone the reverse time button, it should check to see if those marks were errased
      //maybe check to see if there are less filled squares than before
    });
  });