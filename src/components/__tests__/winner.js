import React from "react";
import renderer from "react-test-renderer";

describe('Test Button component', () => {
    it('Test click event', () => {
      // Will the system declare a winner
      //have x's go down the middle with no opposition, will X be declared winner after 5 turns?
    });
  });